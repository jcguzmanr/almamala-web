# Logo

El logo **no va en la tabla de tokens**. Vive aparte.

Archivo canónico (ya en el repo):

`public/images/logo_am.svg`

Al implementar: usarlo en header (y footer si hace falta). No meter la ruta ni un hex del logo dentro de `tokens/colors.json`. No sustituir el pack shot de botella por el logo.
